#!/usr/bin/perl -w

my @prod_servers = ("freyr", "castor", "geb");

foreach my $server (@prod_servers) { 
	print " DEPLOYING TO ".$server;
	system "ssh tabroom\@$server \"cd /www/tabroom; git pull\" ";
}
