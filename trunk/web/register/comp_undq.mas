<%args>
	$tourn
	$comp_id
</%args>
<%init>

	my $comp = Tab::Entry->retrieve($comp_id);

	$comp->dq(0);
	$comp->update;
		
	my $err = "Entryetitor ".$comp->code." UN-DISQUALIFIED";

	$m->redirect("$Tab::url_prefix/register/entry/edit.mhtml?err=$err&comp_id=$comp_id");
	
		
</%init>
