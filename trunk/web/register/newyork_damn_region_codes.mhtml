<%args>
	$circuit
	$tourn
	$session
</%args>
<%perl>

	foreach my $school ($tourn->schools) { 

		my $code = $school->code;

		my $schcode = substr($code,0,2);

		my $region_code = $school->region->code;

		my $new_code = $schcode.$region_code;

</%perl>

		<p><% $school->name." ".$new_code %></p>

<%perl>

		$school->code($new_code);
		$school->update;

	}

</%perl>
