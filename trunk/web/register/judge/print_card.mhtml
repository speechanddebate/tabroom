<%args>
	$tourn
	$session
	$judge_id
</%args>
<%init>

	my $judge = Tab::Judge->retrieve($judge_id);
	my $name = $judge->first." ".$judge->last;
	$name =~ s/[\W_]//g;

	my $filename = "JudgeCard-".$name."-".$session->id;
	my $filepath = $Tab::file_root."/tmp/$filename";
	my $garbage = `rm -f $filename.*`;

	$m->comp("/funclib/printout.mas", filename => $filename, head => 1);

    $m->comp("print/card.mas", judge_id => $judge_id, filename => $filename);

	$m->comp("/funclib/printout.mas", filename => $filename, tail => 1);

</%init>
