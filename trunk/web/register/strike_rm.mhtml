<%args> 
	$strike_id
	$tourn
	$from => undef
</%args>
<%init>

	my $strike = Tab::Strike->retrieve($strike_id);

	my $con_judge = $strike->judge;	

	unless ($tourn->id eq $strike->tournament->id) { 
		$m->print("<h1>I frown upon your shenanigans</h1>");
		$m->print("<p>That strike does not belong to your tournament</p>");
		$m->print("<p>Quit screwing around</p>");
		$m->abort;
	}

	my $msg = "Judge strike ".$strike->name." removed";

	my $judge = $strike->judge;
	$strike->delete;
	$m->redirect("$Tab::url_prefix/register/judge_edit.mhtml?judge_id=".$judge->id."&msg=$msg") if $from eq "edit";
	$m->redirect("$Tab::url_prefix/register/strikes.mhtml?judge_id=".$judge->id."&msg=$msg");

</%init>
