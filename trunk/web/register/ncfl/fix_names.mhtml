<%args>
	$tourn
</%args>
<%perl>


	foreach my $student ($tourn->students) { 

		my $first = $student->first;
		my $last = $student->last;

		$first =~ s/^\s+//;
		$last =~ s/^\s+//;

		$student->first($first);
		$student->last($last);
		$student->update;

	}


</%perl>
