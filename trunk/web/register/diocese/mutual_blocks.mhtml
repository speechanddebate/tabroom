<%args>
	$tourn
	$account
	$session
	$err => undef
</%args>
<%init>

</%init>

<p class="err"><% $err %></p>

<h3>Block two dioceses against each other's judges</h3>

<table cellpadding="5" cellspacing="1" width="100%">

<tr>
<form action="mutual_blocks_save.mhtml" method="post">
<td align="center">
	<h4>Diocese 1</h4>
	<select name="1" size="20">
%		foreach my $diocese (sort {$a->name cmp $b->name} $tourn->regions) { 
			<option value="<% $diocese->id %>"><% $diocese->name %></option>
%		}
	</select>
</td>


<td align="center">
	<h4>Diocese 2</h4>
	<select name="2" size="20">
%		foreach my $diocese (sort {$a->name cmp $b->name} $tourn->regions) { 
			<option value="<% $diocese->id %>"><% $diocese->name %></option>
%		}
	</select>
</td>
</tr>


<tr>
	<td colspan="2" align="center">
	<input class="blue" type="submit" value="    Block Dioceses   ">
	</form>
	</td>
<tr>

</table>
