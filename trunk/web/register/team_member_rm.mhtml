<%args>
	$tourn
	$student_id
	$comp_id
</%args>

<%init>

	foreach my $membership (Tab::EntryStudent->search( student => $student_id, comp => $comp_id  )) { 
		$membership->delete;
	}

	my $err = "Student deleted from team";

    $m->redirect("$Tab::url_prefix/register/entry/edit.mhtml?comp_id=$comp_id&err=$err");


</%init>
