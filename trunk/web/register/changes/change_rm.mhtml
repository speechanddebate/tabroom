<%args>
	$tourn
	$account
	$change_id
</%args>
<%init>
	my $change = Tab::Change->retrieve($change_id);

	my $c_tourn = $change->tournament;

	if ($c_tourn->id eq $tourn->id) { 
		$change->delete;
	}


	$m->redirect("$Tab::url_prefix/register/drops_and_adds.mhtml");
	

</%init>
