<%args>
	$account
	$tourn
</%args>
<%init>

	$m->abort unless $account->site_admin;

	foreach my $school ($tourn->schools) { 

		$school->region($school->chapter_region);
		$school->update;

	}

	my $err = "I did it.";

	$m->redirect("$Tab::url_prefix/register/reports.mhtml?err=$err");

</%init>
