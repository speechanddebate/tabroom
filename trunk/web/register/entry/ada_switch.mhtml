<%args>
	$entry_id
</%args>
<%perl>

	my $entry = Tab::Entry->retrieve($entry_id);

	if ($entry->ada) { 
		$entry->ada("");
	} else { 
		$entry->ada(1);
	}

	$entry->update;

	$m->redirect("$Tab::url_prefix/register/entry/edit.mhtml?entry_id=$entry_id");

</%perl>
