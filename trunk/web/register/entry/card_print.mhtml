<%args>
	$tourn
	$session
	$entry_id
</%args>
<%init>

	my $entry = Tab::Entry->retrieve($entry_id);

	my $filename = $Tab::file_root."/tmp/entry-card-".$entry->code."-".$session->id;
	my $garbage = `rm -f $filename.*`;
	
	open (TEXOUT, ">$filename.tex");

	print TEXOUT <<"EOF";
\\documentclass[10pt]{report}
\\usepackage{fullpage}
\\usepackage{times}
\\usepackage{nopageno}
\\usepackage{colortbl}
\\usepackage[hmargin=.5in,vmargin=.75in]{geometry}
\\renewcommand{\\arraystretch}{1.6}
\\begin{document}
\\begin{center}

EOF

	close TEXOUT;

    $m->comp("/funclib/entry_info_card.mas",
            entry_id => $entry_id,
            filename => $filename);

    open (TEXOUT, ">>$filename.tex");
    print TEXOUT "\\end{center}\n";
    print TEXOUT "\\end{document}\n";
    close TEXOUT;

	$garbage = `cd $Tab::file_root/tmp; $Tab::latex_path $filename.tex; $Tab::dvipdfm_path $filename.dvi`;
	`rm -f $filename.tex $filename.log $filename.dvi $filename.aux`;
	$m->redirect("$Tab::url_prefix/tmp/entry-card-".$entry->code."-".$session->id.".pdf");

</%init>
