<%args> 
	$entry_id
	$seed
</%args>
<%init>

	my $entry = Tab::Entry->retrieve($entry_id);

	$m->abort unless $entry;

	$entry->seed($seed);
	$entry->update;

	my $msg = "Entry ".$entry->code." changed to seed $seed";

	$m->redirect("edit.mhtml?entry_id=$entry_id&msg=$msg");

</%init>
