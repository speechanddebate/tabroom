<%args>
	$tourn
</%args>
<%init> 

	my @schools = $tourn->schools;

	foreach my $school (@schools) {
		my @entries = $school->entries;
		my $num_entries = scalar @entries;
		$school->score($num_entries); 
		$school->update;
	}


</%init> 

