<%args>
	$tourn
</%args>
<%init> 

	my @schools = $tourn->schools;

	foreach my $school (@schools) {
		my @comps = $school->comps;
		my $num_comps = scalar @comps;
		$school->score($num_comps); 
		$school->update;
	}


</%init> 

