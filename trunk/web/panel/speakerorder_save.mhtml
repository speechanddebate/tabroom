<%args>
	$panel_id
	$tourn
</%args>
<%init>

	my $panel = Tab::Panel->retrieve($panel_id);


	foreach my $entry ($panel->entries) {

		my $order = $ARGS{$entry};

		foreach my $ballot ($entry->ballots(panel => $panel->id)) {
			$ballot->speakerorder($order);
			$ballot->update;
		}
	
	}

	$m->redirect("$Tab::url_prefix/panel/panel_view.mhtml?panel_id=$panel_id");


</%init>
