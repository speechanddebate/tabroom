<%args>
	$tourn
	$event_id => undef
	$group_id => undef
</%args>
<%init>

	my $group = Tab::Judge->retrieve($group_id) if $group_id;

	$group = $tourn->groups->first unless $group;

	my $event = Tab::Event->retrieve($event_id) if $event_id;

	$event = $group->events->first unless $event;

	my $switch;

</%init>

	<& menu.mas, tourn => $tourn, group => $group, event => $event, whoami => "availability" &>

	<div class="left huge">

%		if ($group) { 	
			<& tabbar.mas, group => $group, event => $event, whoami => "availability" &>
%		}

		<h4>Judge available in <% $event->abbr %></h4>

%		if ($event) { 	
%		foreach my $round ($event->rounds) { 
	
			<div class="<% ($switch++ % 2) ? "oddrow" : "evenrow" %>">

				<span class="half">
					<% $round->realname %>
				</span>

				<span class="half">
%					my $judge_ref = $m->comp("/funclib/judge_avail_by_rd.mas", round => $round);
					<% scalar @{$judge_ref} %> judges
				</span>

			</div>

%		}
%		}

	</div>

