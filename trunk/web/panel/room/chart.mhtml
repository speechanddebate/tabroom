<%args>
	$tourn
	$site_id
</%args>
<%init>

	my $site = Tab::Site->retrieve($site_id);

	my @timeslots = $m->comp("/funclib/site_timeslots.mas", site => $site, tourn => $tourn);

	my @panels = $m->comp("/funclib/site_rooms.mas", site => $site, tourn => $tourn);



</%init>

	<div class="right small">
		
		<div class="sidenote">

			<h4>Tournament Sites</h4>

%			foreach my $osite ($tourn->sites) { 
				<a class="<% $site->id == $osite->id ? "dk" : "" %>blue block" href="chart.mhtml?site_id=<% $osite->id %>">
					<% $osite->name %>
				</a>
%			}

		</div>

	</div>

	<div class="left huge">


	</div>

