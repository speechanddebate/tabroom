<%args>
	$tourn
</%args>
<%init>

	foreach my $event ($tourn->events) {

		foreach my $round ($event->rounds) { 

			$round->listed($ARGS{$round->id});
			$round->update;
		}

	}

	my $msg = "Lists of clearing competitors settings saved.";

	$m->redirect("/panel/publish_rounds.mhtml?msg=$msg");

</%init>

