<%args>
	$tourn
	$vcorner 
	$hcorner
	$vlabel
	$hlabel
</%args>
<%perl>


	$tourn->hlabel($hlabel);
	$tourn->vlabel($vlabel);
	$tourn->hcorner($hcorner);
	$tourn->hcorner($hcorner);

	$tourn->update;

	my $msg = "Ballot label size adjusted";

	$m->redirect("adjust_labels.mhtml?msg=$msg");

</%perl>

