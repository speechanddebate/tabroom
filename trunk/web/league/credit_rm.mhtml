<%args>
	$credit_id
</%args>
<%init>

	my $credit = Tab::Coach->retrieve($credit_id);

	my $name = $credit->name;
	my $chapter_id = $credit->chapter->id;

	$credit->delete;

	my $err = $name ." removed as a coach";

    $m->redirect("$Tab::url_prefix/circuit/chapter_edit.mhtml?chapter_id=$chapter_id?err=$err");


</%init>	
