<%args> 
	$entry
</%args>
<%init>

	Tab::Student->set_sql( by_entry => 
		"select student.* from student, entry_student
			where student.id = entry_student.student
			and entry_student.entry = ? 
			order by student.last");

	return Tab::Student->search_by_entry($entry->id);

</%init>

