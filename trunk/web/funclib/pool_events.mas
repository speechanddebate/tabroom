<%args>
	$pool
</%args>
<%init>

	return unless $pool;

	Tab::Event->set_sql( by_pool => "
		select distinct event.* 
		from event, round
		where event.id = round.event
		and round.pool = ? 
		order by event.name
	");

	return Tab::Event->search_by_pool($pool->id);

</%init>
