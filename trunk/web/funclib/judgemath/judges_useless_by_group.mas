<%args>
	$school
	$group
</%args>
<%init>

	if ($group->tournament->method->judge_quality_system && $group->coach_ratings) { 

		return Tab::Judge->search_useless_judges(
				$school->id, 
				$group->id);

	}

</%init>
