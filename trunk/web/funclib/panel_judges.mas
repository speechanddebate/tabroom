<%args>
	$panel
</%args>
<%init>

	Tab::Judge->set_sql( by_panel => "
		select distinct judge.* 
		from judge, ballot
		where ballot.judge = judge.id
		and ballot.panel = ?
		order by judge.last");

	return Tab::Judge->search_by_panel( $panel->id );

</%init>

