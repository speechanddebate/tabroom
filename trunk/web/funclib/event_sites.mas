<%args>
	$event
</%args>
<%init>

	Tab::Site->set_sql(by_event => "
        select distinct site.id
        from site,round
        where site.id = round.site
        and round.event = ?");

	return Tab::Site->search_by_event($event->id);


</%init>
