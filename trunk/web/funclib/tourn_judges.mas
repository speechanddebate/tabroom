<%args>
	$tourn
</%args>
<%perl>

	Tab::Judge->set_sql( by_tourn => "
       	select distinct judge.* 
		from judge, school
		where judge.school = school.id
		and school.tourn = ? 
		order by school.name, judge.last");
	
    return Tab::Judge->search_by_tourn($tourn->id);

</%perl>

