<%args>
	$tourn
</%args>
<%init>

    Tab::Round->set_sql( listed => "
        select round.* 
        from round,event,timeslot
        where round.listed = 1
        and round.event = event.id
        and event.tourn = ? 
        and round.timeslot = timeslot.id
        order by timeslot DESC
    ");

    return Tab::Round->search_listed($tourn->id);

</%init>
