<%args>
	$event_id
</%args>
<%init>

	my $event = Tab::Event->retrieve($event_id); 

	foreach my $entry ($event->entries) { 
		my $code = $entry->code;
		$code =~ s/[\D_]//g;
		$entry->code($code);
		$entry->update;
	}

	$m->print("I did it");
	$m->abort;

</%init>

