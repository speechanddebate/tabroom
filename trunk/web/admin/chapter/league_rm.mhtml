<%args>
	$cl_id
</%args>
<%init>

	my $err;
	my $cl = Tab::ChapterCircuit->retrieve($cl_id);

	my $circuit_id = $cl->circuit->id;

	$cl->delete;

	$m->redirect("chapters_search.mhtml?circuit_id=$circuit_id");

</%init>
