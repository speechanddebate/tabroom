<%args>
	$circuit_id => undef
	$account
	$session
	$link_id => undef
</%args>
<%init>

	my $circuit = Tab::Circuit->retrieve($circuit_id);

</%init>

	<& circuit_list.mas, circuit_id => $circuit_id &>

	<div id="circuitpage">

		<& circuit_header.mas, circuit => $circuit, link_id => $link_id &>

		<% $m->call_next %>

	</div>

	<div style="clear: both;" />

