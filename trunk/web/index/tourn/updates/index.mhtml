<%args>
	$account => undef
	$tourn_id
	$event_id => undef
	$group_id => undef
</%args>
<%init>

	use POSIX;

	my $tourn =  Tab::Tourn->retrieve($tourn_id);
	my $event = Tab::Event->retrieve($event_id) if $event_id;  
	my $group = Tab::JudgeGroup->retrieve($group_id) if $group_id;

	my ($entref, $judgeref, $schref) = $m->comp("/funclib/account_follows.mas", tourn => $tourn, account => $account)  if $account;

	my $switch;

</%init>

	<& sidebar.mas, tourn_id => $tourn_id, event_id => $event_id, group_id => $group_id &>

	<div class="left huge">

		<div>
			<span class="threequarters">
				<h2>
					<% $tourn->name %> 
				</h2>
			</span>

			<span class="quarter rightalign top">
				<h4>
					<% $tourn->location %>  
				</h4>
			</span>
		</div>

		<& /index/tourn/tourn_header.mas, tourn => $tourn, account => $account &>

%		if ($account) { 

			<div>
				<span class="half">
					<h4>Who you are following:</h4>
				</span>

				<span class="half rightalign">
					<p>Tap to remove</p>
				</span>
			</div>

%			foreach my $entry (@{$entref}) { 
				<div class="half marno">
					<a class="smallish nospace yellow full" title="<% $entry->code %>" href="update_remove.mhtml?event_id=<% $event_id %>&entry_id=<% $entry->id %>&tourn_id=<% $tourn_id %>">
						<span class="quarter padleft">
							Entry
						</span>
						<span class="half">
							<% $entry->code %>
						</span>
						<span class="quarter nowrap">
							<% $entry->name %>
						</span>
					</a>
				</div>
%			}

%			foreach my $judge (@{$judgeref}) { 
				<div class="half marno">
					<a class="smallish nospace yellow full" title="<% $judge->first." ".$judge->last %>" href="update_remove.mhtml?judge_id=<% $judge->id %>&tourn_id=<% $tourn_id %>">
						<span class="quarter padleft">
							Judge
						</span>
						<span class="half">
							<% $judge->first." ".$judge->last %>
						</span>
						<span class="quarter nowrap">
							<% $judge->code %>
						</span>
					</a>
				</div>
%			}

%			foreach my $school (@{$schref}) { 
				<div class="half padno">
					<a class="smallish nospace yellow full" title="<% $school->name %>" href="update_remove.mhtml?school_id=<% $school->id %>&tourn_id=<% $tourn_id %>">
						<span class="quarter padleft">
							School
						</span>
						<span class="half">
							<% $school->name %>
						</span>
						<span class="quarter nowrap">
							<% $school->code %>
						</span>
					</a>
				</div>
%			}

%		}

%		if ($event) { 

			<div>

				<span class="half">
					<h4><% $event->name %> entries:</h4>
				</span>

				<span class="half">
					<p>Click on a student to follow their live updates</p>
				</span>
			</div>

%				my @entries = sort {$a->school->name cmp $b->school->name} $event->entries ( dropped => 0, waitlist => 0);
%				my $total = scalar @entries;
%				my $count;

				<div class="half">

%				foreach my $entry (@entries) {

%					if ($count == ceil($total /2)) {
						</div>
						<div class="half">
%					}

%					$count++;

					<a class="smallish blue full" title="<% $entry->code %>" href="entry.mhtml?entry_id=<% $entry->id %>&tourn_id=<% $tourn_id %>">
						<span class="half nowrap">
							<% $entry->code %>
						</span><span class="padleft half nowrap">
							<% $entry->name %>
						</span>
					</a>

%				}

				</div>
%		}

%		if ($group) { 

			<div>
				<span class="half">
					<h4><% $group->name %> judges</h4>
				</span>
 
				<span class="half rightalign">
					<p>Click on a judge to follow their live updates</p>
				</span>
			</div>

%				my @judges = sort {$a->last cmp $b->last} $group->judges( active => 1);
%				@judges = sort {$a->school->short_name cmp $b->school->short_name} @judges;
%				my $total = scalar @judges;
%				my $count;

				<div class="half">

%				foreach my $judge (@judges) {

%					if ($count == ceil($total /2)) {
						</div>
						<div class="half">
%					}

%					$count++;

					<a class="smallish blue full" href="judge.mhtml?judge_id=<% $judge->id %>&tourn_id=<% $tourn_id %>">

						<span class="half nowrap">
						<% $judge->first." ".$judge->last %>
						</span>
						<span class="half nowrap">
							<% ($judge->school && $judge->school->id) ? $judge->school->short_name : "Hired"  %>
						</span>
					</a>

%				}

				</div>


%		}

	</div>


