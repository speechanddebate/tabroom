<%args>
	$group_id => undef
</%args>
<%init>

	my $group = Tab::JudgeGroup->retrieve($group_id) if $group_id;  
	my $tourn = $group->tourn if $group;

	my $switch;

	use POSIX;

</%init>

	<& sidebar.mas, tourn_id => $tourn->id, group_id => $group_id, &>

	<div class="left huge">

		<h3><% $group->name %> judges</h3>

%		my @judges = sort {$a->last cmp $b->last} $group->judges ( active => 1);
%		my $total = scalar @judges;
%		my $count;

		<div style="width: 20em; float: left; margin-right: 10px; margin-left: 10px;">

%			foreach my $judge (@judges) {

%				if ($count == ceil($total /2)) {
					</div>
					<div style="width: 20em; float: left;">
%				}

%				$count++;

				<a class="smallish blue block" href="judge.mhtml?judge_id=<% $judge->id %>&tourn_id=<% $tourn->id %>">
					<% $judge->code %>
					<% $judge->first." ".$judge->last %> 
				</a>

%			}

		</div>

	</div>


