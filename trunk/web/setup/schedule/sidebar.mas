<%args>
	$tourn
	@days => undef
	$today => undef
	$event => undef
</%args> 

	<div id="rightsmall">

		<a href="print.mhtml" class="redblock">Print Master Schedule</a>
		
		<br />

		<h4>Set up timeslots</h4>

%		foreach my $tday (@days) { 

			<a href="sked.mhtml?date=<% $tday->mdy('/') %>" 
				class="<% ($today && $tday->dmy == $today->dmy) ? "chosenblock" : "blueblock" %>">
					<% $tday->day_name %> (<% $tday->month."/".$tday->day %>)
			</a>

%		}

		<br />

		<h4>Assign events to timeslots</h4>

%		foreach my $ev (sort {$a->name cmp $b->name} $tourn->events ) { 

			<a href="event.mhtml?event_id=<% $ev->id %>" class="<% ($event && $event->id == $ev->id) ? "chosenblock" : "blueblock" %>">
				<% $ev->name %>
			</a>

%		}

	</div>

