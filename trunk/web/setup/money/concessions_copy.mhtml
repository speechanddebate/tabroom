<%args>
	$tourn
	$from_id
</%args>
<%init>


	my @concessions = Tab::Concession->search( tourn => $from_id);

	foreach (@concessions) {
		my $new = $_->copy({
			tourn => $tourn->id
		});
	}

	$m->redirect("concessions.mhtml");


</%init>
