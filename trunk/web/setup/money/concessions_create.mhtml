<%args>
	$tourn
	$session
	$account
	$new_date
	$new_time
	$new_name
	$new_price
	$new_description => undef
</%args>
<%perl>

    my $deadline = Tab::dtme($new_date,$new_time);

 	my $new_item = Tab::Item->create({
		tournament => $tourn->id,
		name => $new_name,
		price => $new_price,
		description => $new_description,
		deadline => $deadline
	});

	my $msg = "Item $new_name created";

	$m->redirect("concessions.mhtml?msg=$msg");

</%perl>

