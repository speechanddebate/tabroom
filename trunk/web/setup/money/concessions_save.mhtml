<%args>
	$tourn
	$session
	$account
</%args>
<%perl>

	my @items = sort {$a->name cmp $b->name} $tourn->items;
	my $switch;

	foreach my $item (@items) { 

	    my $deadline = Tab::dtme($ARGS{$item->id."_date"},$ARGS{$item->id."_time"});

		$deadline->set_time_zone($tourn->league->timezone);

		$item->name( $ARGS{$item->id."_name"} );
		$item->price( $ARGS{$item->id."_price"} );
		$item->description( $ARGS{$item->id."_description"} );
		$item->deadline($deadline);
		$item->update;

	}

	my $msg = "Updates saved";

	$m->redirect("concessions.mhtml?msg=$msg");

</%perl>

