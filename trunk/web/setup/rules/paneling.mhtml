<%args>
	$account
	$tourn
	$err => undef
</%args>
<%init>

	my $switch;

</%init>

    <& sidebar.mas, tourn => $tourn, chosen => "paneling" &>

    <div class="main">

		<h2>Paneling Settings</h2>

		<p>All of these settings have moved into the events/division settings
		so they may be set on a per-event basis</p>



	</div>
