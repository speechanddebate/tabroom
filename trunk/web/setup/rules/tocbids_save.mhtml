<%args>
	$tourn
</%args>
<%init>

	  foreach my $event ($tourn->events) {
		$event->setting("bid_round", $ARGS{$event->id});
	  }

	  $m->redirect("tocbids.mhtml?msg=Bid level changes saved");

</%init>
