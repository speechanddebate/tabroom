<%args>
$tourn
</%args>

<%init>
  my $switch;

  my $tz = $tourn->tz;
  $tz = "UTC" unless $tz;
</%init>

<div class="main">

  <h2>TOC Bid Level</h2>

  <& tabbar.mas, tourn => $tourn, whoami => "tocbids" &>
  <form action="tocbids_save.mhtml" method="post">

%   foreach my $event ($tourn->events) {
      <div class="optionblock <% ($switch++ % 2) ? "odd" : "even" %>">
        <span class="third ">
          <% $event->name %>
        </span>

        <span class="twothird smaller rightalign">
          <select name="bid_rounds" class="fixedmed">
            <option></option>
            <option value="<% $event->id %>:" >
              None
            </option>
%           foreach my $round ($event->rounds) {
%             if ($round->type eq "final" or $round->type eq "elim") {
                <option value="<% $event->id %>:<% $round->id %>" >
                  <% $round->label %>
                </option>
%             }
%           }
          </select>
        </span>

      </div>
%   }

  <div class="liblrow block rightalign">
    <input type="submit" value="Save Settings">
    </form>
  </div>
