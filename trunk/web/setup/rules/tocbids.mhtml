<%args>
$tourn
</%args>

<%init>
  my $switch;

  my $tz = $tourn->tz;
  $tz = "UTC" unless $tz;
</%init>

<div class="main">

  <h2>Bids</h2>

  <& tabbar.mas, tourn => $tourn, whoami => "tocbids" &>

  <h4>Tournament of Champions Bid Reporting</h4>

  <p>To auto-report bids to the Tournament of Champions, select the bid level
  of the qualifying divisions:</p>

  <form action="tocbids_save.mhtml" method="post">

%   foreach my $event ($tourn->events) {
      <div class="optionblock <% ($switch++ % 2) ? "odd" : "even" %>">
        <span class="third ">
          <% $event->name %>
        </span>

        <span class="twothird smaller rightalign"> 
			
			<select name="<% $event->id %>" class="fixedmed" placeholder="Select round of bid level">

				<option value="">
					No bid
				</option>

%	           foreach my $round ($event->rounds) {
%	             	if ($round->type eq "final" or $round->type eq "elim") {
						<option value="<% $round->id %>" <% $round->id == $event->setting('bid_round') ? "selected" : "" %>>
							  <% $round->label %>
						</option>
%	             	}
%	          	}
			</select>
        </span>

      </div>
%   }

  <div class="liblrow block rightalign">
    <input type="submit" value="Save Settings">
    </form>
  </div>
