<%args>
	$tourn
	$name
</%args>
<%init>

	Tab::TiebreakSet->create({
		tourn => $tourn->id,
		name => $name
	});

	my $msg = "Tiebreak set $name created";

	$m->redirect("tiebreaks.mhtml?msg=$msg");


</%init>
