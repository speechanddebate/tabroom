<%args>
	$tourn
	$name
	$type
</%args>
<%init>

	Tab::TiebreakSet->create({
		tourn => $tourn->id,
		name => $name,
		type => $type
	});

	my $msg = "Tiebreak set $name created";

	$m->redirect("tiebreaks.mhtml?type=$type&msg=$msg");


</%init>
