<%args>
	$league
	$chosen => "registration"
</%args>

	<div id="rightsmall">

		<h4>Rules</h4>

		<a class="<% ($chosen eq "registration") ? "chosenblock" : "blueblock" %>" href="registration.mhtml">
			Registration
		<a>

		<a class="<% ($chosen eq "messages") ? "chosenblock" : "blueblock" %>" href="messages.mhtml">
			Messages & Warnings
		<a>

		<a class="<% ($chosen eq "paneling") ? "chosenblock" : "blueblock" %>" href="paneling.mhtml">
			Paneling
		</a>

%		if ($league->track_bids) { 
			<a class="<% ($chosen eq "bids") ? "chosenblock" : "blueblock" %>" href="bids.mhtml">
				Bids
			</a>
%		}

		<a class="<% ($chosen eq "tabbing") ? "chosenblock" : "blueblock" %>" href="tabbing.mhtml">
			Tabulation
		</a>

		<a class="<% ($chosen eq "tiebreaks") ? "chosenblock" : "blueblock" %>" href="tiebreaks.mhtml">
			Tiebreakers
		</a>

		<a class="<% ($chosen eq "sweeps") ? "chosenblock" : "blueblock" %>" href="sweeps.mhtml">
			Sweepstakes
		</a>

%		if ($chosen eq "tiebreaks" || $chosen eq "explain") { 
		
			<br />

			<a href="tiebreaks_explain.mhtml" class="<% ($chosen eq "explain") ? "chosenblock" : "redblock" %>">
				Guide to Tiebreakers
			</a>

%		}

	</div>


