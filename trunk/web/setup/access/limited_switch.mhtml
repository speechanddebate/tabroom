<%args>
	$staff_id
</%args>
<%perl>

	my $staff = Tab::AccountAccess->retrieve($staff_id);

	if ($staff->entry) { 

		$staff->entry(0);

	} else {

		$staff->entry(1);

	}

	$staff->update;

	my $msg = "Limited settings changed for ".$staff->account->first." ".$staff->account->last;

	$m->redirect("access.mhtml?msg=$msg");

</%perl>
