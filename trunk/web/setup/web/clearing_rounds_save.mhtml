<%args>
	$tourn
	$session
</%args>
<%perl>


	foreach my $event ($tourn->events) {

		foreach my $round ($event->rounds) {

			$round->listed("");
			$round->listed(1) if $ARGS{$round->id};
			$round->update;

		}
	}

	my $msg = "Lists of clearing students saved";

	$m->redirect("/setup/web/clearing.mhtml?msg=$msg");


</%perl>
