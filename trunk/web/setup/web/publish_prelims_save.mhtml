<%args>
	$tourn
</%args>
<%perl>

	foreach my $p ($tourn->prelims) { 

		$p->published(1);
		$p->update;

	}

	my $msg = "Prelim rounds all published.";

	$m->redirect("/setup/web/postings.mhtml?msg=$msg");


</%perl>
