<%args>
	$tourn
	$account
	$event_id => undef
</%args>
<%init>

	my $event = Tab::Event->retrieve($event_id) if $event_id;

</%init>

	<& menu.mas, tourn => $tourn, event => $event &>

	<div class="left huge">


	</div>
