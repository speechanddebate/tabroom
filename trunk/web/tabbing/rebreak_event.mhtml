<%args>
	$tourn
	$circuit
	$account
	$certain => undef
</%args>
<%init>

	my @events = sort {$a->name cmp $b->name} $m->comp("/funclib/tourn_events.mas", tourn => $tourn);

</%init>

% if ($certain eq "I am certain") { 

<h3>Dump panels & re-break event</h3>

<table cellpadding="5" width="100%">

%	my $switch;

%	foreach my $event (@events) { 

%		my @rounds = sort {$b->name <=> $a->name} $event->rounds( preset => 0);
%		next unless @rounds;
%		my $round = shift @rounds;
%		next if $round->type eq "prelim";

		<tr <% ($switch++ % 2) ? "class=\"oddrow\"" : "class=\"evenrow\"" %>>

			<th><% $event->name %></th>

			<td>Clearable round:</td>

			<td>
				Round <% $round->name %>
				<% ($round->label) ? "(".$round->label.")" : "" %> 
			</td>

			<td align="center">
				<form action="clear_round.mhtml" method="post">
				<input type="hidden" name="round_id" value="<% $round->id %>">
				<input type="submit" value="   Clear competitiors only   ">
				</form>
			</td>

			<td align="center">
				<form action="round_rm.mhtml" method="post">
				<input type="hidden" name="round_id" value="<% $round->id %>">
				<input type="submit" value="   Delete entire round   ">
				</form>
			</td>


		</tr>

%	}

</table>

%	} else {

<&  "/funclib/warning.mas", account => $account &>

<h3></h3>
<p></p>
<br><br>
<br><br>
<P align="center"><B>

	The following screen will allow you to dump panel assignements and delete
	rounds altogether for various elimination rounds and events.  It is thus
	very dangerous.

</p>

<p align="center">
	If this is what you intend to do, you can do it, but there'll
	be no getting the data back if you screw up.
</p>

<p align="center">Don't say I didn't warn you. </b></p>

% if ($certain) {

<p><font color="red">You did not type "I am certain", exactly like that.  Try
again</font></p>

% }

<br><br>
<p align="center">To proceed, type "I am certain" in the box below:</p>
<br><br>
<form action="rebreak_event.mhtml" method="post">

	<p align="center">
	<input type="text" name="certain" size="20">
	</p>

	<p align="center">
	<input  type="submit" value="Delete Round">
	</p>
</form>




%	}





