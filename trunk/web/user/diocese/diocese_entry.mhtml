<%args>
	$account
	$diocese_id
	$tourn_id
</%args>
<%init>

	my $diocese = Tab::Region->retrieve($diocese_id);
	my $tourn = Tab::Tournament->retrieve($tourn_id);

	my @chapters = sort {$a->name cmp $b->name} $diocese->chapters;

</%init>

	<div id="leftbig">

		<h2>Nationals entry for <% $diocese->name %></h2>

		<p>
			Click on events at left to enter students & judges
		</p>

		<p>
			Judge categories in red are below obligation; those in green are OK
		</p>


	</div>

	<& sidebar.mas, diocese => $diocese, tourn => $tourn &>

