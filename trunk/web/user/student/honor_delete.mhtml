<%args>
	$honor_id
</%args>
<%init>

	my $honor = Tab::Result->retrieve($honor_id);

	my $student = $honor->student;

	my $msg = "Honor ".$honor->honor." deleted.";

	$honor->delete;

	$m->redirect("/user/student/index.mhtml?student_id=".$student->id."&msg=$msg");


</%init>
