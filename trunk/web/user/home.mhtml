<%args>
	$account
	$session
	$chapter_id => undef
	$tourn_id => undef
	$circuit_id => undef
</%args>
<%init>

	my $chapter;

	if ($chapter_id) { 

		$chapter = Tab::Chapter->retrieve($chapter_id);

	} else { 

		my @chapters = $account->chapters;
		$chapter = shift @chapters if scalar @chapters == 1;

	}

</%init>

	<div id="leftbig">

%		if ($chapter) { 

%			$m->redirect("/user/chapter/tournaments.mhtml?chapter_id=".$chapter->id);

%		} else { 

			<h2>Welcome to Tabroom.com</h2>

			<p>
				If this is the first time your school/team has registered for a
				tournament on tabroom.com, you should first create your school.
				Click on "Create a school/team" at right.
			</p>

			<p>
				If you are a new coach of a school/team that already has used
				tabroom.com, then someone needs to add you to that school's
				account.  An existing coach can add you.  If you cannot contact
				previous coaches, send an email to <a href="mailto:<%
				$Tab::admin_email %>"><% $Tab::admin_email %></a> and we'll be
				happy to add you to an existing program.
			</p>

			<p>
				If you want to run a tournament yourself, click on "Request
				a tournament" at right.
			</p>


%		}

	</div>

	<div id="rightsmall">

		<& menu.mas, chapter => $chapter, account => $account &> 

	</div>

