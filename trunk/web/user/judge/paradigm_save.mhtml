<%args>
	$account
	$paradigm => undef
</%args>
<%init>

	my $tz = $account->tz;
	$tz = "UTC" unless $tz;

	my $now = DateTime->now(time_zone => $tz);

	use HTML::Scrubber;

	if ($paradigm) { 

		$account->setting("paradigm", "text", $paradigm);
		$account->paradigm_timestamp($now);
		$account->paradigm(1);
		$account->update;

	} else { 

		$account->setting("paradigm", "");
		$account->paradigm_timestamp($now);
		$account->paradigm("");
		$account->update;
	}


	$account->update;

	my $msg = "Paradigm saved";
	$m->redirect("/user/judge/paradigm.mhtml?msg=$msg");


</%init>
