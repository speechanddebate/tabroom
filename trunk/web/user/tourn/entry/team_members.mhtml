<%args>
	$circuit
	$comp_id
	$err => undef
</%args>
<%init>

	use POSIX;

	my $comp = Tab::Comp->retrieve($comp_id);

	my @clean_students = $m->comp("/funclib/students_evententer.mas", 
			event => $comp->event, 
			circuit => $circuit, 
			tourn => $comp->tournament, 
			chapter => $comp->school->chapter);

	@clean_students = sort {ucfirst($a->last) cmp ucfirst($b->last)} @clean_students;

</%init>

	<div id="leftbignb">
	
		<h2>Team members in <% $comp->name %></h2>

		<table cellpadding="5" cellspacing="1" width="100%">

%			my $switch;
	
%			foreach my $member ($comp->members) { 

				<tr <% ($switch++ % 2) ? "class=\"oddrow\"" : "class=\"evenrow\"" %>>

					<td>
						<% $switch %>.
					</td>

					<td>
						<% $member->first." ".$member->last %>
					</td>
							
					<td class="right">
						<a class="whiteblock" href="team_members_rm.mhtml?comp_id=<% $comp->id %>&school_id=<% $comp->school->id %>&student_id=<% $member->id %>">
							Remove from team
						</a>
					</td>

				</tr>

%			}

		</table>

	</div>

	<div id="rightsmall">

		<a class="blue block" href="students.mhtml?event_id=<% $comp->event->id %>&school_id=<% $comp->school->id %>">
			Return to <% $comp->event->name %> entry
		</a>

		<h4>Add students:</h4>

		<form action="team_members_save.mhtml">

			<input type="hidden" name="comp_id" value="<% $comp_id %>">
			<input type="hidden" name="school_id" value="<% $comp->school->id %>">

			<center>

			<select name="student_id" onchange='this.form.submit()' size="30">

%			foreach my $student (@clean_students) { 

				<option value="<% $student->id %>">
					<% $student->first." ".$student->last %>
				</option>

%			}	


			</select>

			</center>


            <noscript><td><input type="submit" value="Show"></td></noscript>

		</form>

	</div>

