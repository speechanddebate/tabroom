<%args>
	$account
</%args>
<%init>

	my $now = DateTime->now();

	my @circuits = $m->comp("/funclib/account_circuits.mas", account => $account);

	return unless @circuits;

</%init>

	<h4>Circuits</h4>

%		foreach my $circuit (@circuits) { 

			<a class="blue block" href="/user/circuit/circuit_select.mhtml?circuit_id=<% $circuit->id %>">
				<% $circuit->name %>
			</a>

%			my @approvals = $m->comp("/funclib/circuit_tourns.mas", circuit => $circuit, approval => 1);

%			if (@approvals) { 
			
				<a class="yellow block" href="/circuit/approvals.mhtml?circuit_id=<% $circuit->id %>">
					>>> <% scalar @approvals %> tournaments need approval
				</a>

%			}

%		}
