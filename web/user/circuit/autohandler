<%args>
	$circuit_id => undef
	$account
</%args>
<%init>

	my $circuit = Tab::Circuit->retrieve($circuit_id) if $circuit_id;

	if ($circuit) { 

		my @ca = Tab::CircuitAdmin->search( account => $account->id, circuit => $circuit_id);

		unless ($account->site_admin || @ca) { 
			$m->print("<p>I call shenanigans</p>");
			$m->abort;
		}

		$m->call_next( circuit => $circuit );

	} else { 

		$m->call_next;

	}

</%init>

