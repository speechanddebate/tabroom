<%args>
	$staff_id
	$circuit_id
	$tourn_id
</%args>
<%perl>

	my $staff = Tab::TournAdmin->retrieve($staff_id);

	if ($staff->entry_only) { 

		$staff->entry_only(0);

	} else {

		$staff->entry_only(1);

	}

	$staff->update;

	my $msg = "Limited settings changed for ".$staff->account->first." ".$staff->account->last;

	$m->redirect("tourn_edit.mhtml?tourn_id=$tourn_id&circuit_id=$circuit_id&msg=$msg");

</%perl>
