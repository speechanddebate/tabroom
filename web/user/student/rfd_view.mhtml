<%args>
	$person
	$entry_id
	$panel_id
	$judge_id
</%args>
<%init>

	my $panel = Tab::Panel->retrieve($panel_id);
	my $judge = Tab::Judge->retrieve($judge_id);
	my $entry = Tab::Entry->retrieve($entry_id);

	my @scores = $m->comp("/funclib/panel_scores.mas", 
					panel => $panel,
					judge => $judge,
					entry => $entry);

	my @comments = $m->comp("/funclib/panel_scores.mas", 
					panel    => $panel,
					judge    => $judge,
					entry    => $entry,
					comments => 1
				);

</%init>

	<& /user/menu.mas, whoami => "student", person => $person &>

	<div class="main">

		<div class="block">
			<span class="seveneighths">
				<h4><% $panel->round->event->tourn->name %></h4>
			</span>
			<span class="eighth rightalign">
				<a class="centeralign bluetext strong button buttonwhite fa-2x fa fa-home" href="/user/home.mhtml">
				</a>
			</span>
		</div>

		<hr/> 

		<div class="full">

			<span class="threefifths nospace">
				<h3><% $entry->code %></h3>
				<h5><% $panel->round->realname %> decision from <% $judge->first." ".$judge->last %></h5>
			</span>

			<span class="twofifths">

				<span class="quarter">
%					foreach my $score (@scores) { 
%						next unless $score->tag eq "ballot";
						<h5> <% $score->value == 1 ? "WIN" : "LOSS" %></h5>
%					}
				</span>

				<span class="threequarters">

%				foreach my $student ($entry->students) { 

					<div class="full lightrow">
						<span class="third padless marno">
							<% $student->last %>:
						</span>

%						foreach my $score (@scores) { 

%							next unless $score->tag eq "points" 
%										&& $score->student 
%										&& $score->student->id == $student->id;

							<span class="third padless marno">
								<% $score->value %>
							</span>
%						}

%						foreach my $score (@scores) { 

%							next unless $score->tag eq "rank" 
%								&& $score->student 
%								&& $score->student->id == $student->id;

							<span class="third padless marno">
								<% $score->value %>
							</span>
%						}

					</div>

%				}

				</span>

			</span>

		</div>

%		foreach my $comment (@comments) { 

%			if ($comment->tag eq "rfd") { 
				<div class="lightrow full">
					<% $comment->content %>
				</div>
%			} 

%			if ($comment->tag eq "comments") { 
				<h4>Comments specific to <% $comment->student->last %>:</h4>
				<div class="lightrow full">
					<% $comment->content %>
				</div>
%			}

%		}	


	</div>


