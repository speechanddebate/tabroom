<%args> 
	$target_id    => undef
	$nsda_strikes => 0
</%args>
<%init>

	my $tourn = Tab::Tourn->retrieve($target_id);

	$tourn->setting("nsda_strikes", $nsda_strikes);

	Tab::debuglog("Tourn ".$tourn->name." strikes are $nsda_strikes");

	return;

</%init>

