<%args>
	$district_id
</%args>
<%init>

	my $district = Tab::District->retrieve($district_id);

	my $msg = $m->comp("/funclib/nsda/districts_import.mas", district_id => $district->id) if $district;

	$m->redirect("district_edit.mhtml?district_id=$district_id&msg=$msg");

</%init>
