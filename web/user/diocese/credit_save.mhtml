<%args>
	$chapter_id
	$tourn_id => undef
	$name
	$err => undef
</%args>
<%init>

	unless ($name eq "Add new coach") {   # Don't ask.

		my $credit = Tab::Coach->create({ 
			name => $name,
			chapter => $chapter_id
		});

		$err = $name." added as a coach";
	
	}

	$m->redirect("$Tab::url_prefix/user/diocese/chapter_edit.mhtml?chapter_id=$chapter_id&tourn_id=$tourn_id&err=$err");

</%init>
