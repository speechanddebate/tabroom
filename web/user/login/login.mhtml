<%args>
	$tourn_id => undef
	$username => undef
	$return   => undef
	$person   => undef
	$session  => undef
</%args>
<%init>

	unless ($person && $session) { 

		foreach my $fleh ("vd", "vq", "vl", "vg", "fq") { 

			my $fuckcookie = Apache2::Cookie->new(
				$r,
				-name     => $fleh,
				-value    => '',
				-expires  => '-19s',
				-domain   => 'www.tabroom.com',
				-httponly => 1,
				-path     => '/'
			);

			$fuckcookie->bake($r);

			$fuckcookie = Apache2::Cookie->new(
				$r,
				-name     => $fleh,
				-value    => '',
				-expires  => '-19s',
				-domain   => '.tabroom.com',
				-httponly => 1,
				-path     => '/'
			);

			$fuckcookie->bake($r);
		}

		my $tokencookie = Apache2::Cookie->new(
			$r,
			-name     => 'TabroomToken',
			-value    => '',
			-secure   => 0,
			-domain   => '.tabroom.com',
			-expires  => '-19s',
			-httponly => 1,
			-path     => '/'
		);

		$tokencookie->bake($r);

		$tokencookie = Apache2::Cookie->new(
			$r,
			-name     => 'TabroomToken',
			-value    => 'WHEE',
			-secure   => 0,
			-expires  => '-Thu, 01 Jan 1970 00:00:00 GMT',
			-path     => '/'
		);

		my $fun = $tokencookie->bake($r);

		Tab::debuglog("$fun");

	}

</%init>


	<div class="main">

	<h2>Log In</h2>

	<p class="semibold redtext centeralign">
		If you've been getting the "Session expired" error message, logging in
		here should help!
	<p>

	<p class="semibold redtext centeralign">
		If that still doesn't work try clearing the cookies from your brower
		settings.
	</p>

	<form 
		action = "login_save.mhtml"
		method = "post"
	>

		<input 
			type  = "hidden"
			name  = "tourn_id"
			value = "<% $tourn_id %>"
		>

		<input 
			type  = "hidden"
			name  = "category_id"
			value = "<% $ARGS{"category_id"} %>"
		>

		<input 
			type  = "hidden"
			name  = "return"
			value = "<% $return %>"
		>

		<div class="row">

			<span class="third">
				User:
			</span>

			<span class="twothirds">
		
				<input 
					type  = "email"
					name  = "username"
					size  = "32"
					value = "<% $username %>"
				>
			</span>

		</div>

		<div class="row">

			<span class="third">
				Password:
			</span>

			<span class="twothirds">
				<input 
					type = "password"
					name = "password"
					size = "32"
				>
			</span>

		</div>

		<div class="liblrow rightalign">
			<input 
				type="submit" 
				value=" Log Into Your Account "
			>
		</div>

	</form>

		<a
			class = "martopmuchmore centeralign semibold redtext"
			href  = "/user/login/new_user.mhtml"
		>Create a New Account</a> * 

		<a
			class = "martopmuchmore centeralign semibold redtext"
			href="/user/login/forgot.mhtml"
		>Forgotten Password?</a>


	</div>

	<div class="menu">

		<br />

		<h4>Having trouble?</h4>

		<p style="padding: 5px;">

			Be sure that you're using the email address you used when you first
			signed up for an account; tabroom.com doesn't know when you get a
			new address.</p>

		<p style="padding: 5px;">

			If you're totally unable to log in, please email <% $Tab::admin_email %> 
			with your name, school name and the text of any error messages you
			might get.  And remember, tabroom.com is a volunteer effort; we
			don't have paid staff to answer your email, so it might be a couple
			days before I can get back to you
		
		</p>


	</div>
	
