<%args>
	$tourn_id
	$chapter_id
	$account
</%args>
<%init>

	my $nope = Tab::AccountIgnore->create({
		account => $account->id,
		tournament => $tourn_id});

	my $msg = "You will not see that vile, filthy tournament again!  <br /> <br /> Unless you hit the Show All link, of course";

	$m->redirect("tournaments.mhtml?chapter_id=$chapter_id&msg=$msg");

</%init>

