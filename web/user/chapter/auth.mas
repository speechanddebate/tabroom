<%args>
	$chapter
	$account
	$session
</%args>
<%init>

	my @access = Tab::ChapterAdmin->search( chapter => $chapter->id, account => $account->id );

	return if @access;

	my $err = "You are not authorized to make changes to ".$chapter->name;

	$m->redirect("/user/home.mhtml?err=$err");

</%init>
