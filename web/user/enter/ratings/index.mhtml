<%args>
	$school_id
	$group_id => undef 
	$account
	$style => undef
	$entry_id => undef
	$prefs => undef
</%args>
<%init>

	my $school = Tab::School->retrieve($school_id) if $school_id;

	my $group = Tab::JudgeGroup->retrieve($group_id) if $group_id;

	my $tourn = $school->tourn;

	use POSIX;

	my $entry = Tab::Entry->retrieve($entry_id) if $entry_id;

	my $now = DateTime->now;

	my $switch;

</%init>

	<& sidebar.mas, school => $school, whoami => "ratings" &>

	<div class="left huge">

		<h2>Prefs & Strikes at the <% $tourn->name %></h2>

%		unless ($prefs) {
			<& ../menubar.mas, school => $school, whoami => "ratings" &>
%		}

		<p> Please select a judge group & ratings method right to continue</p>

	</div>

