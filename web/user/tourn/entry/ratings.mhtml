<%args>
	$school_id
	$group_id => undef 
	$account
	$style => undef
	$comp_id => undef
</%args>
<%init>

	my $school = Tab::School->retrieve($school_id) if $school_id;

	my $group = Tab::JudgeGroup->retrieve($group_id) if $group_id;

	my $tourn = $school->tournament;

	use POSIX;

	my $comp = Tab::Entry->retrieve($comp_id) if $comp_id;

	my $now = DateTime->now;

	my $switch;

</%init>

	<h2><% $school->name %> at the <% $tourn->name %></h2>

	<& /user/tourn/entry/menubar.mas, school => $school, whoami => "ratings" &>

	<div class="right small">

		<& ratings/sidebar.mas, school => $school, whoami => "ratings" &>

	</div>

	<div class="left huge">
	
		<h2>Judge Ratings</h2>

		<p> Please select a judge group and ratings at right to continue</p>

	</div>

