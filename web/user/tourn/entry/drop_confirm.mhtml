<%args>
	$comp_id
	$school_id
	$h_warn => undef
	$wl_warn => undef
	$add_warn => undef
	$caphit => undef
</%args>
<%init>

	my $comp = Tab::Comp->retrieve($comp_id);

	unless ($comp) { 
		my $err = "That competitior does not exist; did you already drop this entry?";
		$m->redirect("students.mhtml?school_id=$school_id&err=$err");
	}


</%init>

    <& menubar.mas, school => $comp->school, whoami => "students" &>

	<div class="left huge">

%		if ($add_warn) { 
			
			<h3>This event is past its add deadline</h3>

			<div style="padding: 10px;">

			<p>If you drop this student, you will not be able to replace the
			slot with another student</p>

			<p>If you want to simply give this slot to another student, instead
			you should do a name change.  You can do a name change on the entry
			screen by clicking on the student name you want to swap to another
			student.</p>

			<p>If you do truly want to drop this entry slot for good, click
			confirm below.</p>

			</div>

%		}

%		if ($h_warn) { 

			<h3>This entry has housing requested</h3>

			<div style="padding: 10px;">

			<p>The housing requests will automatically be eliminated if you
			drop the entry.  You will not be able to transfer the housing
			request to another entry.  If housing at the tournament is full,
			you may lose your housing slot altogether.</p>

			<p>If you want to keep this housing slot, transfer the housing
			request to another student via the "Housing" tab above; then come
			back and drop the student.</p>

			<p>Otherwise, click "Confirm" below to continue</p>

			</div>


%		}

%		if ($wl_warn &! $add_warn) { 

			<h3>This event is on its waitlist. </h3>

			<div style="padding: 10px;">
			
			<p>If you drop this student, you may lose the student's slot in
			this event at the tournament.</p>

			<p>If you want to simply change this entry to another student,
			instead you should do a name change.  You can do a name change on
			the entry screen by clicking on the student name you want to swap
			to another student.</p>

			<p>If you do truly want to give up this entry slot, click confirm
			below.</p>

			</div>

%		}


		<table cellpadding="5" width="100%">
			<tr class="lirdrow">
				<td align="right">
					<form action="comp_drop.mhtml" method="post">
					<input type="hidden" name="comp_id" value="<% $comp_id %>">
					<input type="hidden" name="school_id" value="<% $school_id %>">
					<input type="hidden" name="confirmed" value="Yep">
					<input type="submit" class="red" value="    Confirm Drop   ">
					</form>
				</td>
			</tr>
		</table>



	</div>

	<div class="right small">

        <h3>Entry</h3>

        <a class="red block">
			<% ($comp->event->no_codes) ? "" : "Competitor code: ".$comp->event->code %>
		</a>

        <a class="red block">
			<% $comp->team_name %>
		</a>

        <a class="red block" href="students.mhtml?school_id=<% $comp->school->id %>&event_id=<% $comp->event->id %>">
        	<% $comp->event->name %>
		</a>

	</div>



