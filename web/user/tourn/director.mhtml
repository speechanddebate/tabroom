<%args>
	$tourn_id
	$school_id
	$session
</%args>
<%init>

	$session->school($school_id);
	$session->director($school_id);
	$session->update;

	$m->redirect("$Tab::url_prefix/user/tourn/entry/entry.mhtml");

</%init>
