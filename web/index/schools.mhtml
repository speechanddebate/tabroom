<%args>
	$tourn_id
</%args>
<%perl>

	use POSIX;

	my $tourn = Tab::Tournament->retrieve($tourn_id);

	unless ($tourn->method->publish_schools) { 
		$m->print("<p class=\"err\">This tournament's list of registered schools is not published.  Please stop messing around with the tabroom.com system.  Palmer is watching.</p>");
	} else { 

	my @schools = sort {$a->chapter->name cmp $b->chapter->name } $tourn->schools;
	my $count = scalar @schools;
	my $half = ceil($count/2);

</%perl>

% my $switch;;

	<h1><% $tourn->name %></h1>

	<form action="index.mhtml" method="post">
		<input type="hidden" name="tourn_id" value="<% $tourn->id %>">
		<input style="margin-top: 5px; padding-top: 2px;" class="skinny" type="submit" value="<< Back">
	</form>

	<h3><% scalar (@schools) %> Attending Schools</h3>

	<table cellpadding="5" cellspacing="1" width="100%">

% 		foreach my $school (@schools) { 

			<tr <% ($switch++ % 2) ? "class=\"oddrow\"" : "class=\"evenrow\"" %>>

				<td>
					<% $school->name %> <% ($school->chapter->state) ? "(".$school->chapter->state.")": "" %>
				</td>

			</tr>

%		}

	</table>

%	}

	</div>
	<& tourn_list.mas, tourn_id => $tourn_id &>

    <div style="clear: both;" />

    </div>



