<%args>
	$person => undef
	$circuit
</%args>
<%init>

</%init>

	<div class="menu">

		<div class="sidenote">

			<h4>
				Circuit
			</h4>

			<a class="odd full">
				<% $circuit->name %> (<% $circuit->location %>)
			</a>

			<a class="even full">
				<% scalar $circuit->tourns %> total tournaments
			</a>

%			if ($circuit->setting("website")) { 
				<a class="yellow block" href="<% $circuit->setting("website") %>">
					Circuit Website
				</a>
%			}

%			if ($circuit->webpages(published => 1)) { 
%				foreach my $webpage ( sort {$a->page_order->epoch <=> $b->page_order->epoch} $circuit->webpages(published => 1)) { 
					<a class="blue" href="index.mhtml?webpage=<% $webpage->id %>">
						<% $webpage->title %>
					</a>
%				}
%			}

		</div>

	</div>
