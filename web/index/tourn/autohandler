<%args>
	$person   => undef
	$session  => undef
	$tourn_id => undef
	$site_id  => undef
	$webname  => undef
</%args>
<%init>

    unless ($tourn_id || $webname) { 
		$m->print("<br /><br /><h3>Error: You have not selected a tournament</h3>");
		$m->abort; 
	}

	if ($tourn_id && ($tourn_id != int($tourn_id) )) { 
		$m->print("<br /><br /><h3>Invalid tournament ID or URL</h3>");
		$m->abort; 
	}

</%init>

	<% $m->call_next %>

