<%args>
	$tourn_id => undef
	$account
	$session
	$link_id => undef
	$news_id => undef
</%args>
<%init>

    unless ($tourn_id) { 
		$m->print("<br /><br /><h3>Error: You have not selected a tournament</h3>");
		$m->abort; 
	}


	my $tourn = Tab::Tourn->retrieve($tourn_id);

</%init>


	<div id="tournpage">

%		if ($tourn) {
	
			<& tourn_header.mas, tourn => $tourn, link_id => $link_id, news_id => $news_id &>
	
			<% $m->call_next %>

%		} else { 

			<h3>This tournament has not yet been set up.</h3>

			<p>Check back later for more information</p>

%		}

	</div>

	<div style="clear: both;" />
