<%args>
	$tourn_id
	$event_id => undef
	$group_id => undef
	$tz
</%args>
<%init>

	use POSIX;

	my $tourn =  Tab::Tournament->retrieve($tourn_id);
	my $event = Tab::Event->retrieve($event_id) if $event_id;  
	my $group = Tab::JudgeGroup->retrieve($group_id) if $group_id;

	my $switch;

</%init>

	<& sidebar.mas, tourn_id => $tourn_id, event_id => $event_id, group_id => $group_id &>

	<div id="lefthuge">

%		if ($event) { 

			<h3><% $event->name %> entries</h3>

			<p>Click on a student to follow their live updates</p>

%				my @comps = sort {$a->school->name cmp $b->school->name} $event->comps ( dropped => 0, waitlist => 0);
%				my $total = scalar @comps;
%				my $count;

				<div style="width: 300px; float: left; margin-right: 10px; margin-left: 10px;">

%				foreach my $comp (@comps) {

%					if ($count == ceil($total /2)) {
						</div>
						<div style="width: 300px; float: left;">
%					}

%					$count++;

					<a class="blueblock" href="entry.mhtml?comp_id=<% $comp->id %>&tourn_id=<% $tourn_id %>">
						<span class="spacey" style="width: 160px;">
							<% $comp->team_name %> 
						</span>
							<% $comp->school->short_name %> 
					</a>

%				}

				</div>


%		}


%		if ($group) { 

			<h3><% $group->name %> judges</h3>
 
			<p>Click on a judge to follow their live updates</p>

%				my @judges = sort {$a->school->short_name cmp $b->school->short_name} $group->judges( active => 1);
%				my $total = scalar @judges;
%				my $count;

				<div style="width: 300px; float: left; margin-right: 10px; margin-left: 10px;">

%				foreach my $judge (@judges) {

%					if ($count == ceil($total /2)) {
						</div>
						<div style="width: 300px; float: left;">
%					}

%					$count++;

					<a class="blueblock" href="judge.mhtml?judge_id=<% $judge->id %>&tourn_id=<% $tourn_id %>">
						<span class="spacey" style="width: 145px;">
							<% $judge->last." ".$judge->first %>
						</span>
							<% ($judge->school && $judge->school->id) ? $judge->school->short_name : "Tourn Hire"  %>
					</a>

%				}

				</div>


%		}

	</div>


