<%args>
	$person
</%args>
<%init>

	unless ($person) {
		my $err = "Because of the prevalance of ineffecient AI spiders putting high levels of load on Tabroom, you must now log in to access that area.";
		$m->redirect("/user/login/login.mhtml?err=$err");
	}

	$m->call_next();

</%init>
