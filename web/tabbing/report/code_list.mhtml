<%args> 
	$tourn
</%args>
<%init> 

	use POSIX;

	my @entries = $m->comp("/funclib/tourn_entries.mas", tourn => $tourn);
	@entries = sort { $a->code <=> $b->code } @entries;

	my @schools = $tourn->schools;
	@schools = sort { $a->code cmp $b->code } @schools;

	my $num_entries = scalar @entries;
	my $quarter = ceil($num_entries / 5);
	my $count; 

</%init>

	<div class="blankfull">

		<h2>Codebreaker for <% $tourn->name %> (<% $num_entries %> entries)</h2> 
				
		<div class="quarter inline">

% 		foreach my $entry (@entries) { 
			
%			if ($count > $quarter) { 
%				undef ($count);
				</div>
				<div class="quarter inline" style="vertical-align: top;">
%			}

%			if ($tourn->setting("ncfl")) { 
				<span class="codespan">
					<% $entry->code %>   <% $entry->school->region->code %>
				</span>
%			} else { 
				<span class="codespan">
					<% $entry->code %>   <% $entry->school->code %>	
				</span>
%			}
				<br class="clear" />

%			$count++;

% 		} 

		</div>

	</div>

