<%args>
	$session
	$group_id    => undef
	$event_id    => undef
	$timeslot_id => undef
</%args>
<%init>

	$session->event($event_id);
	$session->judge_group($group_id);

	$session->update;

	$m->redirect("index.mhtml?timeslot_id=".$timeslot_id);

</%init>
