<%init>

	my @settings = Tab::StudentSetting->search( tag => 'student_email');

	foreach my $setting (@settings) {

		unless (Email::Valid->address(
			-address => $setting->value,
			-mxcheck => 0 )
		) {
			$setting->delete();
		}
	}

	$m->print("<p>Fin</p>");

</%init>
