<%args>

</%args>
<%init>

	my $counter;

	while ($counter < 10) {
		sleep(1);

		$counter++;
		$m->print("Counter is $counter");
		Tab::debuglog("flushed ".$counter);
		$m->flush_buffer();
	}


</%init>
