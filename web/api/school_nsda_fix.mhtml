<%args>

</%args>
<%init>

	Tab::Chapter->set_sql( members => "
		select chapter.*
		from chapter
		where chapter.nsda > 0
		and chapter.nsda IS NOT NULL
	");

	foreach my $chapter (Tab::Chapter->search_members() ) {
		$chapter->nsda(  $chapter->nsda + 10000 );
		$chapter->update();
	}

</%init>
