<%args>
	$uber_id
	$other_id
</%args>
<%init>

	Tab::Uber->set_sql( switch_judges => "
		UPDATE judge SET uber = $other_id
		WHERE uber = ".$uber_id);

	Tab::Uber->sql_switch_judges->execute;

	my $uber = Tab::Uber->retrieve($uber_id);

	$uber->delete;

	return;

</%init>
