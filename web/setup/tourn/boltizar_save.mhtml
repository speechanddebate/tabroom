<%args>
	$tourn
</%args>
<%init>

	foreach my $key ("boltizar_enable", "boltizar_host_system_code", "boltizar_require_epayments", "boltizar_cc_surcharge") { 
		$ARGS{$key} = 0 unless $ARGS{$key};
		$tourn->setting($key, $ARGS{$key});
	}

	my $msg = "Boltizar payment settings saved";
	$m->redirect("settings.mhtml?msg=".$msg);

</%init>