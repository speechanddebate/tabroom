<%args>
	$tourn
	$name
	$double_entry => 0
	$double_max => undef
</%args>
<%init>

	my $class = Tab::Class->create({
		tournament => $tourn->id,
		name => $name,
		double_entry => $double_entry,
		max => $double_max
	});

	my $msg = "Grouping ".$class->name." created";

	$m->redirect("/setup/events/double_entry.mhtml?msg=$msg");

</%init>

