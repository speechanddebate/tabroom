<%args>
	$tourn
</%args>
<%init>

	foreach my $event ($tourn->events) {
		$event->class($ARGS{$event->id});
		$event->update;
	}

	my $msg = "Event groupings updated";
	$m->redirect("/setup/events/double_entry.mhtml?msg=$msg");

</%init>
