<%args>
	$group_id
	$account
	$certain => undef
</%args>
<%init>
	
	my $group = Tab::JudgeGroup->retrieve($group_id);

	if ($certain eq "I am certain") { 

		my $group = Tab::JudgeGroup->retrieve($group_id);

		my $msg = "Judge Group ".$group->name." deleted";

		$group->delete;

		$m->redirect("/setup/judges/edit.mhtml?msg=$msg");

	}	

</%init>

	<div id="rightsmall">

		<br />
		<br />
		<br />

		<a class="yellowblock" href="/setup/judges/edit.mhtml?group_id=<% $group->id %>">
			NO!  OMG!  DON'T DELETE IT! GO BACK!
		</a>
	</div>

<div class="left huge">

    <form action="group_rm.mhtml" method="post">
    <input type="hidden" value="<% $group->id %>" name="group_id">

        <&  "/funclib/warning.mas", account => $account &>

        <center>
        <P><B>You are about to permanently delete the judge group <% $group->name %></b></p>

		<p>
			The judges, the <% scalar $group->events %> events in it, and all the competitors
			underneath will all be toast.  This cannot be undone.  You can only
			get it back by re-entering the whole thing over again.
		</p>

        <p>
			Don't say I didn't warn you.  Because I just did.
		</p>

%       if ($certain) {

            <p>
                <font color="red">
                    You did not type "I am certain", exactly like that.  Try again
                </font>
            </p>

%       }

        <br>
        <br>

            <p>To proceed, type "I am certain" in the box below:</p>

        <br>
        <br> 
		
		<p align="center">
			<input type="text" name="certain" size="20">
		</p> 
		
		<p align="center">
			<input class="blue" type="submit" value="Yes I am sure I know what I am doing">
       		</form>
		</p>

</div>

