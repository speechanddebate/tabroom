<%args>
	$set_id
	$event_id
</%args>
<%init>

	my $set = Tab::SweepEvent->create({
		sweep_set => $set_id,
		event => $event_id
	});

	my $msg = "Event added";
	$m->redirect("sweeps.mhtml?set_id=".$set_id."&msg=$msg");

</%init>

