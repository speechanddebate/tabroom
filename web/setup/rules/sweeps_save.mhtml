<%args>
	$tourn
	$sweep_method => 0
	$sweep_class_based => 0
	$sweep_per_event => 0
	$sweep_event_total => 0
	$sweep_wildcards => 0
	$sweep_final_rank => 0
	$sweep_count_elims => 0
	$sweep_count_finals => 0
	$sweep_count_prelims => 0
	$sweep_rank_in_elims => 0
	$points_per_finalist => 0
	$points_per_elim => 0
	$points_per_entry => 0
	$sweep_only_place_final => 0
	$novice_only => 0
</%args>
<%init> 

	$tourn->setting("sweep_method", $sweep_method);
	$tourn->setting("sweep_class_based", $sweep_class_based);
	$tourn->setting("sweep_per_event", $sweep_per_event);
	$tourn->setting("sweep_event_total", $sweep_event_total);
	$tourn->setting("sweep_wildcards", $sweep_wildcards);
	$tourn->setting("sweep_final_rank", $sweep_final_rank);
	$tourn->setting("sweep_count_elims", $sweep_count_elims);
	$tourn->setting("sweep_count_finals", $sweep_count_finals);
	$tourn->setting("sweep_count_prelims", $sweep_count_prelims);
	$tourn->setting("sweep_rank_in_elims", $sweep_rank_in_elims);
	$tourn->setting("points_per_finalist", $points_per_finalist);
	$tourn->setting("points_per_elim", $points_per_elim);
	$tourn->setting("points_per_entry", $points_per_entry);
	$tourn->setting("novice_only", $novice_only);
	$tourn->setting("sweep_only_place_final", $sweep_only_place_final);

	my $msg = "Sweeps settings saved";
	$m->redirect("sweeps.mhtml?msg=$msg");

</%init> 



