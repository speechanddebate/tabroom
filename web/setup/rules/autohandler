<%args>
	$tourn
	$person
	$session
	$perms
	$tourn_settings
	$person_settings
</%args>
<%init>

	$m->call_next(  
		person          => $person,
		tourn           => $tourn,
		perms           => $perms,
		tourn_settings  => $tourn_settings,
		person_settings => $person_settings
	);

</%init>
