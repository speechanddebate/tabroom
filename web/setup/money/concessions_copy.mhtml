<%args>
	$tourn
	$from_id
</%args>
<%init>


	my @concessions = Tab::Item->search( tournament => $from_id);

	foreach (@concessions) {
		my $new = $_->copy;
		$new->tournament($tourn->id);
		$new->update;
	}

	$m->redirect("concessions.mhtml");


</%init>
