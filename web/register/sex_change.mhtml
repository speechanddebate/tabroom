<%args>
	$student_id
	$school_id 
</%args>
<%perl>

	my $student = Tab::Student->retrieve($student_id);

	my $gender = $student->gender;

	$student->gender("F") if $gender eq "M";
	$student->gender("M") if $gender eq "F";
	$student->update;

	$m->redirect("$Tab::url_prefix/register/housing_school.mhtml?school_id=$school_id#student$student_id");

</%perl>
