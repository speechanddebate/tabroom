<%args>
	$session
	$person
	$tourn
	$tourn_settings
</%args>

	<& "menu.mas",
		tourn          => $tourn,
		session        => $session,
		defaults       => $ARGS{"defaults"},
		tourn_settings => $tourn_settings
	&>

	<div class="main">

		<h2>Reports</h2>

	</div>
