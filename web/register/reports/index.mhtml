<%args>
	$session
	$person
	$tourn
	$tourn_settings
	$perms
</%args>

	<& "menu.mas",
		tourn          => $tourn,
		session        => $session,
		defaults       => $ARGS{"defaults"},
		tourn_settings => $tourn_settings,
		person         => $person,
		perms          => $perms
	&>

	<div class="main">

		<h2>Reports</h2>

	</div>
