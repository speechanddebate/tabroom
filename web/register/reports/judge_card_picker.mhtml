<%args> 
	$tourn 
	$session
</%args>
<%init>

</%init>

	<& "menu.mas", class => $class, tourn => $tourn &>

	<div class="main"> 

		<h2>Judge Cards by Category</h2>

		<form action="tourn_judge_cards.mhtml" method="post">
		
			<select name="category_id">

%				foreach my $category ($tourn->categories) { 

					<option value="<% $category->id %>"
					><% $category->name %></option>

%				}

			</select>


		</form>

	</div>
