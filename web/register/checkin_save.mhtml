<%args>
	$tourn
</%args>
<%init>

	foreach my $school ($tourn->schools) { 

		$school->registered($ARGS{$school->id});
		$school->paid_amount($ARGS{"paid_".$school->id});
		$school->update;

	}

	my $msg = "Registration changes saved";

	$m->redirect("/register/checkin.mhtml?msg=$msg");

</%init>
