<%args>
	$tourn
	$account
	$req_id 
	$school_id => undef
	$sort_by => undef
</%args>
<%init>

	my $req = Tab::Housing->retrieve($req_id);

	$req->delete() if $req;

	$m->redirect("$Tab::url_prefix/register/housing.mhtml?sort_by=$sort_by");

</%init>
