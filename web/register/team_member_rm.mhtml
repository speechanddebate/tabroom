<%args>
	$tourn
	$student_id
	$entry_id
</%args>

<%init>

	foreach my $membership (Tab::EntryStudent->search( student => $student_id, entry => $entry_id  )) { 
		$membership->delete;
	}

	my $err = "Student deleted from team";

    $m->redirect("$Tab::url_prefix/register/entry/edit.mhtml?entry_id=$entry_id&err=$err");


</%init>
