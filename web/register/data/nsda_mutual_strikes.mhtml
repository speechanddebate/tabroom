<%args>
	$tourn
	$person
	$err => undef
</%args>
<%init>
</%init>

	<& "menu.mas",
		person => $person,
		whoami => "nsda_mutual_strikes",
		tourn  => $tourn 
	&>

	<div class="main">

		<h4>Strike two districts' judges against each other:</h4>

		<form 
			action = "nsda_mutual_strikes_save.mhtml"
			method = "post"
		>

		<div class="yellowrow">

			<span class="half centeralign">

				<h5>District the First</h5>

			</span>

			<span class="half centeralign">
				<h5>District the Second</h5>
			</span>

		</div>


		<div class="row">

			<span class="half centeralign">
	
				<select name="district_1" class="fixed">
<%perl>
					foreach my $district (
						sort {$a->name cmp $b->name} 
						$m->comp("/funclib/tourn_districts.mas", tourn => $tourn)
					) { 
</%perl>
						<option value="<% $district->id %>"> <% $district->name %> </option>
%					}
				</select>
					
			</span> 

			<span class="half centeralign">

				<select name="district_2" class="fixed">
<%perl>

					foreach my $district (
						sort {$a->name cmp $b->name} 
						$m->comp("/funclib/tourn_districts.mas", tourn => $tourn)
					) { 

</%perl>
						<option value="<% $district->id %>"> <% $district->name %> </option>
%					}
					
				</select>
					
			</span>

		</div>

		<div class="liblrow rightalign">
			<input  type="submit" value="Block Districts">
			</form>

		</div>

	</div>

