<%args>
	$perms
	$tourn
</%args>
<%init>

	if (
		$perms->{tourn}{$tourn} eq "owner"
		|| $perms->{tourn}{$tourn} eq "tabber"
	) {
		$m->call_next();
	}

	$m->comp("/funclib/abort.mas",
		message => "Your permissions are limited to tabulation and not registration functions"
	);

</%init>
