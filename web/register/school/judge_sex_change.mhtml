<%args>
	$judge_id
	$school_id 
	$gender => undef
</%args>
<%perl>

	my $judge = Tab::Judge->retrieve($judge_id);

	if ($judge->gender eq "F") { 
		$judge->gender("M");
	} else { 
		$judge->gender("F");
	}

	$judge->update;

	$m->redirect("housing.mhtml?school_id=$school_id");

</%perl>
