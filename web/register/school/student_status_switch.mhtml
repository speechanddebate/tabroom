<%args>
	$person
	$tourn
	$school
	$student_id
	$property_value
</%args>
<%init>

	$m->clear_buffer();
	$r->content_type('application/json');

	my $school_status = $school->setting('student_form_confirm');

	if ($student_id && $property_value > 0) {
		$school_status->{$student_id} = 1;
	} elsif ($student_id) {
		delete $school_status->{$student_id};
	}

	$school->setting('student_form_confirm', 'json', $school_status);

	my $response = {
		message => "Student status changed",
		error => \0,
	};

	$m->print(JSON::encode_json($response));
	$m->abort;

</%init>
