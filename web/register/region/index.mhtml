<%args>
	$tourn
	$tourn_settings
	$region => undef
</%args>

	<div class="blankfull">

		<& menubar.mas,
			tourn          => $tourn,
			tourn_settings => $tourn_settings,
			region         => $region,
			whoami         => "summary" 
		&>

	</div>

