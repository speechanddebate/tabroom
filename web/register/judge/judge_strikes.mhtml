<%args>
	$judge_id
	$tourn
</%args>
<%init> 

	my $judge = Tab::Judge->retrieve($judge_id);

    my $start = $tourn->start;
    my $end = $tourn->end;

	$start->set_time_zone($tourn->tz);
	$end->set_time_zone($tourn->tz);

	my $morning = DateTime->new(
		hour => 8,
		minute => 00,
		second => 00,
		year => $start->year,
		month => $start->month,
		day => $start->day,
		time_zone => $tourn->tz
	);

	my $night = DateTime->new(
		hour      => 20,
		minute    => 00,
		second    => 00,
		year      => $end->year,
		month     => $end->month,
		day       => $end->day,
		time_zone => $tourn->tz
	);


	my $switch;

</%init>

%  	unless ($judge->hired) {
		<& "/register/menubar.mas", school => $judge->school, whoami => "judges", tourn => $tourn &>
%	} else { 
		<div class="main"> 
%	}
	
		<h3>
			Strikes for <% $judge->first." ".$judge->last %>
		</h3>

		<h4>Time based</h4>
	
		<form action="strike_save.mhtml" method="post">
		<input type="hidden" value="time" name="type">
		<input type="hidden" value="<% $judge->id %>" name="judge_id">

		<div class="visible <% ($switch++ % 2) ? "odd" : "even" %>">

			<span class="quarter ">
				No rounds between
			</span>

%  			if (scalar $m->comp("/funclib/tourn_days.mas", tourn => $tourn) > 1) { 

				<span class="half ">
%					my $first;
%					foreach my $day ($m->comp("/funclib/tourn_days.mas", tourn => $tourn)) {
						<label for="start_<% $day->ymd %>">
							<span class="quarter hover">
								<input type="radio" name="start_date" value="<% $day->mdy('/') %>" id="start_<% $day->ymd %>"  <% ($first++ < 1) ? "checked" : "" %>>
								<% $day->day_abbr %>
							</span>
						</label>
%   				}
				</span>

%   		} else { 

				<span class="half">
					<input type="hidden" name="start_date" value="<% $start->mdy('/') %>" >
					<input type="hidden" name="end_date" value="<% $start->mdy('/') %>" >
				</span>

%   		}

			<span class="quarter centeralign">
				<& /funclib/timepicker.mas, name => "start_time", size => 10, time => $morning &>
			</span> 
			
		</div>
		
		<div class="<% ($switch++ % 2) ? "odd" : "even" %>">

			<span class="quarter">
				And
			</span>

%   		if (scalar $m->comp("/funclib/tourn_days.mas", tourn => $tourn) > 1) { 

				<span class="half ">
%					my $first;
%					foreach my $day ($m->comp("/funclib/tourn_days.mas", tourn => $tourn)) {
						<label for="end_<% $day->ymd %>">
						<span class="quarter hover">
							<input type="radio" name="end_date" value="<% $day->mdy('/') %>" id="end_<% $day->ymd %>"  <% ($first++ < 1) ? "checked" : "" %>>
								<% $day->day_abbr %>
						</span>
						</label>
%  					}
				</span>
%			}

			<span class="quarter centeralign">
				<& /funclib/timepicker.mas, name => "end_time", size => 10, time => $night &>
			</span>

		</div>

		<div class="libl rightalign">
			<input  type="submit" class="thin" value="Strike">
			</form>
		</div>

%		if ($judge->judge_group->strike_times) { 

			<h4>Fine-bearing time blocks:</h4>
			
			<form action="strike_save.mhtml" method="post">
			<input type="hidden" value="strike_time" name="type"> 
			<input type="hidden" value="<% $judge->id %>" name="judge_id">

%   		foreach my $strike_time ( sort {$a->start->epoch <=> $b->start->epoch} $judge->judge_group->strike_times) {

				<div class="<% ($switch++ % 2) ? "odd" : "even" %>">

					<span class="third">
						<% $strike_time->name %>
					</span>
					
					<span class="third">
						$<% $strike_time->fine %>
					</span>

					<label for="st_<% $strike_time->id %>">
						<span class="third">
							<input type="checkbox" name="strike_time_<% $strike_time->id %>" value="1" <% ($judge) ? ($strike_time->strike($judge) ) ? "checked" : "" : "" %>>
						</span>
					</label>

				</div>
%			}

			<div class="liblrow rightalign">
				<input type="submit" class="thin" value=" Strike ">
				</form>
			</div>

%   	}

		<br />
	
		<h4>Event/division</h4>

		<form action="strike_save.mhtml" method="post">
		<input type="hidden" value="event" name="type"> 
		<input type="hidden" value="<%$judge->id%>" name="judge_id">

		<div class="visible <% ($switch++ % 2) ? "odd" : "even" %>">

			<span class="quarter ">
				No rounds in
			</span>

			<span class="half  centeralign">
				<select name="event" class="chosen fixedmed">
%					foreach my $event ($judge->judge_group->events) { 
						<option value="<% $event->id %>">
							<% $event->name %>
						</option>
%					}
				</select>
			</span> 
			
			<span class="quarter centeralign">
				<input  type="submit" class="thin" value="Strike">
				</form>
			</span>

		</div>

		<form action="strike_save.mhtml" method="post">
		<input type="hidden" value="elim" name="type"> 
		<input type="hidden" value="<%$judge->id%>" name="judge_id">

		<div class="visible <% ($switch++ % 2) ? "odd" : "even" %>">

			<span class="quarter ">
				Elims rounds only in
			</span>

			<span class="half  centeralign">

				<select name="event" class="chosen fixedmed">
%					foreach my $event ($judge->judge_group->events) { 
						<option value="<% $event->id %>">
							<% $event->name %>
						</option>
%					}
				</select>
			</span> 
			
			<span class="quarter centeralign">
				<input  type="submit" class="thin" value="Strike">
				</form>
			</span>

		</div>

		<h4>Competitor conflict</h4>

			<form action="strike_save.mhtml" method="post">
			<input type="hidden" value="student" name="type">
			<input type="hidden" value="<%$judge->id%>" name="judge_id">

			<div class="<% ($switch++ % 2) ? "odd" : "even" %>">
	
				<span class="third ">
					Search by last name:
				</span> 
				
				<span class="half centeralign ">
					<input type="text" name="name" size="30" placeholder="Search by last name">
				</span>

				<span class="sixth centeralign">
					<input  type="submit" class="thin" value="Strike">
					</form>
				</span>
				
			</div> 

			<form action="strike_save.mhtml" method="post">
			<input type="hidden" value="entry" name="type">
			<input type="hidden" value="<%$judge->id%>" name="judge_id">

			<div class="<% ($switch++ % 2) ? "odd" : "even" %> martop">
	
				<span class="third">
					Search by entry code:
				</span> 
				
				<span class="half centeralign ">
					<input type="text" name="code" size="30" placeholder="Search by code">
				</span>

				<span class="sixth centeralign ">
					<input  type="submit" class="thin" value="Strike">
					</form>
				</span>
				
			</div> 


		<br />

		<h4>School conflict</h4>

			<form action="strike_save.mhtml" method="post">
			<input type="hidden" value="school" name="type">
			<input type="hidden" value="<%$judge->id%>" name="judge_id">

			<div class="visible <% ($switch++ % 2) ? "odd" : "even" %>">

				<span class="twothird rightalign">

					<select name="school" class="chosen fixed">
%						foreach my $school (sort {$a->name cmp $b->name} $tourn->schools) { 
%							next unless $school->chapter;
							<option value="<% $school->id %>"> <% $school->short_name %> <% ($school->region) ? "(".$school->region->name.")" : "" %>
							</option>
%						}
					</select>
				</span>

				<span class="third rightalign">
					<input  type="submit" class="thin" value="Strike">
					</form>
				</span>
				
			</div>

% 			if ($tourn->setting("regions")) {

				<h4>Region Conflict</h4>

				<form action="strike_save.mhtml" method="post">
				<input type="hidden" value="region" name="type">
				<input type="hidden" value="<%$judge->id%>" name="judge_id">

				<div class="visible <% ($switch++ % 2) ? "odd" : "even" %>">

					<span class="twothird rightalign">
						<select name="region" class="chosen fixed">
%							foreach my $region (sort {$a->name cmp $b->name} $tourn->regions) { 
								<option value="<% $region->id %>">
									<% $region->name %>
								</option>
%							}
						</select>
					</span>

					<span class="third rightalign">
						<input  type="submit" class="thin" value="Strike">
						</form>
					</span>
				
				</div>
			

%			}


%			if ($tourn->setting("ncfl")) {

				<h4>Diocese Conflict</h4>


				<div class="<% ($switch++ % 2) ? "odd" : "even" %>">

					<span class="third">
						<form action="strike_save.mhtml" method="post">
						<input type="hidden" value="region" name="type">
						<input type="hidden" value="<%$judge->id%>" name="judge_id">
				
						<select name="region">
%						foreach my $region (sort {$a->name cmp $b->name} $m->comp("/funclib/tourn_regions.mas", tourn => $tourn)) { 
							<option value="<% $region->id %>">
								<% $region->name %>
								<% sprintf("%2s", $region->code) %>
							</option>
%						}
						</select>
					</span>

					<span class="third rightalign">
						<input  type="submit" class="thin" value="Strike">
						</form>
					</span>
				
				</div>
		

%			}

	</div>

	<div class="menu">

		<div class="plainnote">

			<a class="yellow block" href="edit.mhtml?judge_id=<% $judge->id %>">
				Return to judge screen
			</a>

		</div>

		<div class="sidenote">

			<h4>
				Existing strikes
			</h4>

			<p>
				Click a strike to delete
			</p>

% 			foreach my $strike (sort {$a->type cmp $b->type} $judge->strikes) { 

				<a class="blue block" href="strike_rm.mhtml?strike_id=<% $strike->id %>">
					<% $strike->name %>
				</a>

%			}

		</div>

	</div>


	<br style="clear: both;">


