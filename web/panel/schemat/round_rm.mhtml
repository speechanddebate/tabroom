<%args>
	$round_id
	$tourn
</%args>
<%init>

	my $round = Tab::Round->retrieve($round_id);

	my $event = $round->event;

	$round->delete;

	$m->redirect("/panel/schemat/show.mhtml?event_id=".$event->id);

</%init>
