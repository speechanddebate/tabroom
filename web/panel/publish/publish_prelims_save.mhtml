<%args>
	$tourn
</%args>
<%perl>

	foreach my $p ($tourn->prelims) { 

		$p->published(1);
		$p->update;

	}

	my $err = "Prelim rounds all published.";

	$m->redirect("$Tab::url_prefix/panel/publish_rounds.mhtml?err=$err");


</%perl>
