<%args>
	$tourn
</%args>
<%perl>

	foreach my $event ($tourn->events) { 

		next if $event->type eq "debate";

		foreach my $round ($event->rounds) { 

			if ($ARGS{$round->id}) { 

				$round->published(1);

			} else { 

				$round->published(0);

			}

			$round->update;

		}

	}

	$m->redirect("$Tab::url_prefix/panel/publish_rounds.mhtml");

</%perl>
