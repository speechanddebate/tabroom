<%args> 
	$circuit
	$group_id
</%args>
<%init>

	my $group = Tab::JudgeGroup->retrieve($group_id);
	
	my @judges = $group->judges;

	foreach my $judge (@judges) { 

		my $num_cs = scalar $judge->cannot_judge;
		$judge->score($num_cs);
		$judge->update;

	}	


</%init>
