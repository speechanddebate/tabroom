<%args>
	$pool_id
	$round_id
</%args>
<%init>
	
	my $round = Tab::Round->retrieve($round_id);
	$round->pool("");
	$round->update;

	my $msg = $round->realname." will now draw from all judges, not just this pool";
	$m->redirect("pool.mhtml?pool_id=$pool_id&group_id=".$round->event->judge_group->id."&msg=$msg");


</%init>
