<%args>
	$pool_id
</%args>
<%init>

	my $pool = Tab::Pool->retrieve($pool_id);

	my $msg = "All judges from ".$pool->name." removed";

	Tab::PoolJudge->set_sql( dump_pools => " delete from pool_judge where pool = ? ");

	Tab::PoolJudge->sql_dump_pools->execute($pool_id);

	$m->redirect("pool.mhtml?pool_id=$pool_id&msg=$msg&group_id=".$pool->judge_group->id);


</%init>
