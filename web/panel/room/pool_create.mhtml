<%args>
	$tourn
	$rg_name => undef
</%args>
<%init>

	if ($rg_name) { 

		my $pool = Tab::RoomGroup->create({
			tourn => $tourn->id,
			name  => $rg_name,
		});

		my $msg = "Pool $rg_name create";
		$m->redirect("pool.mhtml?pool_id=".$pool->id."&msg=$msg");

	}

</%init>

	<& menu.mas, tourn => $tourn,  whoami => "pool"  &>

	<div class="left huge">

		<h2>Create a room pool</h2>

		<h4>Give it a name:</h4>

		<div class="evenrow block padmore">
			<form action="pool_create.mhtml" method="post">
			<input type="text" name="rg_name" size="30">
			<input type="submit" class="thin" value=" That's it.  Create it" style="margin-left: 20px;">
			</form>
		</div>

	</div>
