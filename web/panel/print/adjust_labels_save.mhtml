<%args>
	$tourn
	$vcorner 
	$hcorner
	$vlabel
	$hlabel
</%args>
<%perl>


	$tourn->setting("hlabel", $hlabel);
	$tourn->setting("vlabel", $vlabel);
	$tourn->setting("hcorner", $hcorner);
	$tourn->setting("vcorner", $vcorner);

	$tourn->update;

	my $msg = "Ballot label size adjusted";

	$m->redirect("index.mhtml?msg=$msg");

</%perl>

