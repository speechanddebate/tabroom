<%args>
	$tourn
</%args>
<%init>

	Tab::RoomPool->set_sql(by_tourn => "
		select distinct room_pool.*
		from room_pool,event 
		where event.tourn = ?
		and room_pool.event = event.id
		order by event.name
		");
	

	return Tab::RoomPool->search_by_tourn($tourn->id);


</%init>
