<%args>
	$string => undef
	$dt     => undef
	$tz     => undef
	$length => "short"
</%args>
<%init>

	unless ($dt) {
		$dt = eval {
			return DateTime::Format::MySQL->parse_datetime($string);
		};
	}

	my $string;

	if ($dt) {

		$dt->set_time_zone($tz) if $tz;

		if ($ARGS{"day_abbr"}) {
			$string .= $dt->day_abbr." ");
		}

		if ($length eq "sortable") {

			$string .= $dt->ymd."-".$dt->hms('-');

		} elsif ($length eq "csv") {

			$string .= $dt->year."-".sprintf("%02d",$dt->month)."-".sprintf("%02d",$dt->day);
			$string .= " ".sprintf("%02d",$dt->hour).":".sprintf("%02d",$dt->strftime('%M'));

		} elsif ($length eq "casual") {

			$string .= $dt->day_abbr." ".$dt->hour_12.":".$dt->strftime("%M")." ".$dt->strftime('%p');

		} elsif ($length eq "shortest") {

			my $string .= $dt->month."/".$dt->day." ".$dt->hour.":".$dt->strftime("%M";

		} elsif ($length eq "shorter") {

			$string .= $dt->month."/".$dt->day." ".$dt->hour_12.":".$dt->strftime("%M").substr($dt->strftime('%p'),0,1);

		} elsif ($length eq "short") {

			$string .= $dt->month_abbr." ".$dt->day." ".$dt->hour_12.":".$dt->strftime("%M")." ".$dt->strftime('%p');

		} elsif ($length eq "medium" || (not defined $length)) {

			$string .= $dt->day." ".$dt->month_abbr." ".$dt->year." ";
			$string .= $dt->hour_12.":".$dt->strftime("%M")." ".$dt->strftime('%p');

		} elsif ($length eq "long") {

			$string .= $dt->day." ".$dt->month_name." ".$dt->year." ";
			$string .= $dt->hour_12.":".$dt->strftime("%M")." ".$dt->strftime('%p');

		} elsif ($length eq "longer") {

			$string .= $dt->day_abbr." ".$dt->day." ".$dt->month_name." ".$dt->year." ";
			$string .= $dt->hour_12.":".$dt->strftime("%M")." ".$dt->strftime('%p');

		} elsif ($length eq "murica") {

			$string .= $dt->day_abbr." ".$dt->month_name." ".$dt->day.", ".$dt->year." ";
			$string .= $dt->hour_12.":".$dt->strftime("%M")." ".$dt->strftime('%p');

		} elsif ($length eq "longest") {

			$string .= $dt->day_name." ".$dt->day." ".$dt->month_name." ".$dt->year." at ";
			$string .= $dt->hour_12.":".$dt->strftime("%M")." ".$dt->strftime('%p');
		}
	}

	return $string if $ARGS{return};
	$m->print($string);
	return;

</%init>
