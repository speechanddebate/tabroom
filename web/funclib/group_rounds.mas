<%args>
	$group
</%args>
<%init>

	Tab::Round->set_sql( by_group => "
		select distinct round.* 
		from round, event 
		where round.event = event.id
		and event.judge_group = ? 
		order by round.name
	");

	return Tab::Round->search_by_group($group->id);

</%init>
