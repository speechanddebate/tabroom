<%args>
	$tourn
</%args>
<%perl>

	Tab::Account->set_sql(by_follow_tourn =>
		"select person.* from person, follow_tourn
		where person.id = follow_tourn.account
		and follow_tourn.tourn = ? 
		order by person.email");

	return Tab::Account->search_by_follow_tourn($tourn->id);

</%perl>
