<%args>
	$event
</%args>
<%init>

	Tab::TiebreakSet->set_sql( by_event => "
		select distinct tiebreak_set.*
		from tiebreak_set, round
		where round.event = ? 
		and round.tiebreak_set = tiebreak_set.id
	");

	return Tab::TiebreakSet->search_by_event($event->id);

</%init>
