<%args>
	$tourn
</%args>
<%perl>

	Tab::Event->set_sql( by_tourn => "
       	select distinct me.* 
       	from event me, judge_group
       	where me.judge_group = judge_group.id
       	and judge_group.tourn = ? 
       	order by me.name");

    return Tab::Event->search_by_tourn($tourn->id);

</%perl>

