<%args>
	$tourn
</%args>
<%init>

	Tab::File->set_sql( by_tourn => "
		select distinct file.* 
		from file, event
		where file.result = 1
		and file.event = event.id
		and event.tourn = ?
		order by timestamp desc
	");

	return Tab::File->search_by_tourn($tourn->id);


</%init>
