<%args>
	$round => undef
</%args>
<%init>

	my $event = $round->event;
	my $now = DateTime->now();

	Tab::Autoqueue->create({
		tag       => "scores",
		event     => $event,
		round     => $round,
		active_at => $now,
	});

	return;

</%init>
