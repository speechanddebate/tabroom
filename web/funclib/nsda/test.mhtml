<%args>
	$tourn
	$person => undef
</%args>
<%init>

	my $event = Tab::Event->retrieve(111072);

	my $status = $m->comp(
		"post_points.mas",
		event => $event,
		debug => 1
	);

	$m->clear_buffer();
	$m->print("<pre>");
	$m->print("$status");
	$m->abort("</pre");

</%init>
