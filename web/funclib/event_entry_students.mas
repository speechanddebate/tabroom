<%args>
	$event
</%args>
<%init>

	return unless $event > 0;

	Tab::EntryStudent->set_sql( by_event => "
		select distinct entry_student.*
		from entry_student, entry
		where entry.event = ? 
		and entry.id = entry_student.entry
	");

	return Tab::EntryStudent->search_by_event($event);

</%init>
