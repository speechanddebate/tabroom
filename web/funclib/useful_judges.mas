<%args>
	$school
	$group
</%args>
<%init>


	if ($group->tournament->method->judge_quality_system) { 

		return Tab::Judge->search_usable_judges($school->id, $group->id);
	
	} else { 

		return Tab::Judge->search( judge_group => $group->id,
						school => $school->id );
	}

</%init>
