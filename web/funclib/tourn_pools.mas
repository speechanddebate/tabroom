<%args>
	$tourn
</%args>
<%perl>

	Tab::Pool->set_sql( by_tourn => "
       	select distinct pool.* 
		from pool, judge_group
		where pool.judge_group = judge_group.id
		and judge_group.tourn = ?
		order by judge_group.name, pool.name");

    return Tab::Pool->search_by_tourn($tourn->id);

</%perl>

