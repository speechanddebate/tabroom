<%args>
	$text => undef
</%args>
<%doc>

	I think this might be a total disaster but here we go.

</%doc>
<%init>

	my $strip = HTML::Strip->new();
	$text = $strip->parse($text);
	$text = lc($text);

    my $word_count;
    $word_count++ while $text =~ /\S+/g;
    return $word_count;

</%init>
