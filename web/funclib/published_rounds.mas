<%args>
	$tourn
</%args>
<%init>

    Tab::Round->set_sql( published => "
        select round.* 
        from round,event,timeslot
        where round.published > 0
        and round.event = event.id
        and event.tourn = ? 
        and round.timeslot = timeslot.id
        order by timeslot DESC
    ");

    return Tab::Round->search_published($tourn->id);

</%init>
