<%args>
	$pool
</%args>
<%init>

	
	Tab::Timeslot->set_sql( by_pool => "
		select distinct timeslot.*
		from timeslot, round
		where timeslot.id = round.timeslot
		and round.pool = ?
		order by timeslot.start
	");

	return Tab::Timeslot->search_by_pool($pool->id);


</%init>
