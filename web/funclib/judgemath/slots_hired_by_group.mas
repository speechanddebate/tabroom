<%args>
	$school
	$group
</%args>
<%init>

	my $request = $school->requests_by_group($group);
	my $hires_requested += $request->covers if $request;
	my $hires_accepted += $request->accepted if $request;

	return $hires_accepted;

</%init>
