<%args>
	$session
	$account => undef
	$league => undef
</%args>

	<& /user/login/account_required.mas, account => $account, session => $session &>

% 	if ($account && $account->site_admin) { 

		<div id="content">

%  			$m->call_next(account => $account, league => $league);

			<div style="clear: both;">

		</div>

% } else { 

% 		$m->print("WHOA THERE, COWBOY.  YOU REALLY DO NOT WANT TO MESS AROUND IN THERE");
%		$m->abort();

% }

