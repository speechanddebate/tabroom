<%args>
	$err => undef
	$edit_id
	$first => undef
</%args>

%	my $edit = Tab::Account->retrieve($edit_id);

%	my $switch;

	<div id="leftbig">

	<h2><% $edit->first." ".$edit->last %>'s Circuits</h2>

	<table cellpadding="5" width="100%" border="0">

%		foreach my $circuit ($edit->circuits) { 

			<tr <% ($switch++ % 2) ? "class=\"oddrow\"" : "class=\"evenrow\"" %>>

				<th>
					<% $circuit->name %>
				</th>

				<td align="right">
					<form action="/admin/circuit_edit.mhtml" method="post">
					<input type="hidden" name="circuit_id" value="<% $circuit->id %>">
					<input type="submit" value="  Edit Circuit  ">
					</form>
				</td>

			</tr>

%		}

	</table>

</div>


	<& account_menu.mas, edit_id => $edit_id &>

