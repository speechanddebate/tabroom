<%args> 
	$circuit
	$tourn
</%args>
<%init> 

	my @entries = $tourn->entries;
	@entries = sort { $a->code <=> $b->code } @entries;
	my @schools = $tourn->schools;
	@schools = sort { $a->code cmp $b->code } @schools;

	my $num_entries = scalar @entries;
	use POSIX;
	my $third = ceil($num_entries / 7);
	my $count; 

</%init>

<table width="100%">
<tr>
<td width="100%">
	<h3>Codebreaker for <% $tourn->name %></h3> 
</td>
<td>
	<form action="print_codebreaker.mhtml" method="post">
	<input type="submit" value="  Print Codebreaker  ">
	</form>
</td>
</tr>
</table>

<table width="100%" cellpadding="5">
<tr>
<td valign="top">
<table>

% foreach my $comp (@entries) { 

%	$count++;

%	if ($count >= $third) { 
		</table>
		</td>
		<td valign="top">
		<table>
%		$count = 0;
% }

%	if ($circuit->diocese_based) { 
		<tr><td><% $comp->code %></td><td><% $comp->school->region->code %></td></tr>
%	} else { 
		<tr><td><% $comp->code %></td><td><% $comp->school->code %></td></tr>
%	}

% } 

</table>

</tr>
</table>


