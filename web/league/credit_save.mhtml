<%args>
	$chapter_id
	$name
</%args>

<%init>
	my $credit = Tab::Coach->create({ 
		name => $name,
		chapter => $chapter_id
	});

	my $err = $name." added as a coach";

	$m->redirect("$Tab::url_prefix/league/chapter_edit.mhtml?chapter_id=$chapter_id?err=$err");

</%init>
